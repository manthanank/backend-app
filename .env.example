# Server Configuration
PORT=3000
NODE_ENV=development
# Domain for generating URLs in emails and responses (without protocol)
# Examples: localhost:3000, backend-app-manthanank.vercel.app
DOMAIN=localhost:3000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/your-database-name

# CORS Configuration
# Comma-separated list of allowed origins (e.g., http://localhost:3000,https://example.com)
ALLOWED_ORIGINS=http://localhost:3000

# Email Configuration (SMTP)
# For Gmail: Use smtp.gmail.com with port 465 (SSL) or 587 (TLS)
# Generate app-specific password: https://myaccount.google.com/apppasswords
SMPT_HOST=smtp.gmail.com
SMPT_PORT=465
SMPT_MAIL=your-email@gmail.com
SMPT_APP_PASS=your-app-password

# JWT Configuration (if using JWT authentication)
JWT_SECRET=your-secret-key-here
JWT_EXPIRE=7d

# Notion API (if using Notion integration)
NOTION_TOKEN="your-notion-token"
NOTION_DATABASE_ID=your-notion-database-id

# Other API Keys (add as needed)
# API_KEY=your-api-key

